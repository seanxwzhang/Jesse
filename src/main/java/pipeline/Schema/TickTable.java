package pipeline.Schema;

public class TickTable extends Schema {
    public static String tableName = "TICKS";
    // TODO: finish the rest of the TickTable class
//    public static String schemaSQL =
//            "CREATE TABLE IF NOT EXISTS "
//                + tableName
//                + "(SYMBOL VARCHAR(10)    PRIMARY KEY    NOT NULL, "
//                + "TSTZ    TIMESTAMPZ     NOT NULL, "
//                + "OPEN    REAL           NOT NULL, "
//                + "CLOSE   REAL           NOT NULL, "
//                + "HIGH    REAL           NOT NULL, "
//                + "LOW     REAL           NOT NULL, "
//                + "COUNT   INT            NOT NULL, "
//                + "VOL     BIGINT         "
}
